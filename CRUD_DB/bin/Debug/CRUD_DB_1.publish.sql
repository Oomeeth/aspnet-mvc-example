/*
Deployment script for crud_db

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "crud_db"
:setvar DefaultFilePrefix "crud_db"
:setvar DefaultDataPath "C:\Users\user-pc\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"
:setvar DefaultLogPath "C:\Users\user-pc\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column [dbo].[Books].[Description] is being dropped, data loss could occur.

The column [dbo].[Books].[Year] is being dropped, data loss could occur.

The column [dbo].[Books].[BookDescription] on table [dbo].[Books] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column [dbo].[Books].[BorrowedByUserId] on table [dbo].[Books] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column [dbo].[Books].[ReleaseYear] on table [dbo].[Books] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[Books])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
/*
The column [dbo].[Library].[Name] is being dropped, data loss could occur.

The column [dbo].[Library].[LibraryName] on table [dbo].[Library] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[Library])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
/*
The column [dbo].[Users].[Address] is being dropped, data loss could occur.

The column [dbo].[Users].[Password] is being dropped, data loss could occur.

The column [dbo].[Users].[UserAddress] on table [dbo].[Users] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column [dbo].[Users].[UserPassword] on table [dbo].[Users] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[Users])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Starting rebuilding table [dbo].[Books]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_Books] (
    [Id]               INT           IDENTITY (1, 1) NOT NULL,
    [Title]            NVARCHAR (50) NOT NULL,
    [Author]           NVARCHAR (50) NOT NULL,
    [BookDescription]  NVARCHAR (70) NOT NULL,
    [ReleaseYear]      INT           NOT NULL,
    [LibraryId]        INT           NOT NULL,
    [IsAvailable]      BIT           NOT NULL,
    [BorrowedByUserId] INT           NOT NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[Books])
    BEGIN
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Books] ON;
        INSERT INTO [dbo].[tmp_ms_xx_Books] ([Id], [Title], [Author], [LibraryId], [IsAvailable])
        SELECT   [Id],
                 [Title],
                 [Author],
                 [LibraryId],
                 [IsAvailable]
        FROM     [dbo].[Books]
        ORDER BY [Id] ASC;
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Books] OFF;
    END

DROP TABLE [dbo].[Books];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_Books]', N'Books';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Starting rebuilding table [dbo].[Library]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_Library] (
    [Id]             INT           IDENTITY (1, 1) NOT NULL,
    [LibraryName]    NVARCHAR (50) NOT NULL,
    [Location]       NVARCHAR (50) NOT NULL,
    [OperatingHours] NVARCHAR (50) NOT NULL,
    [Phone]          NVARCHAR (50) NOT NULL,
    [Email]          NVARCHAR (50) NOT NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[Library])
    BEGIN
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Library] ON;
        INSERT INTO [dbo].[tmp_ms_xx_Library] ([Id], [Location], [OperatingHours], [Phone], [Email])
        SELECT   [Id],
                 [Location],
                 [OperatingHours],
                 [Phone],
                 [Email]
        FROM     [dbo].[Library]
        ORDER BY [Id] ASC;
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Library] OFF;
    END

DROP TABLE [dbo].[Library];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_Library]', N'Library';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Starting rebuilding table [dbo].[Users]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_Users] (
    [Id]           INT           IDENTITY (1, 1) NOT NULL,
    [FirstName]    NVARCHAR (50) NOT NULL,
    [LastName]     NVARCHAR (50) NOT NULL,
    [Phone]        NVARCHAR (10) NOT NULL,
    [Email]        NVARCHAR (50) NOT NULL,
    [UserAddress]  NVARCHAR (50) NOT NULL,
    [UserPassword] NVARCHAR (15) NOT NULL,
    [AccountType]  NVARCHAR (50) NOT NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[Users])
    BEGIN
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Users] ON;
        INSERT INTO [dbo].[tmp_ms_xx_Users] ([Id], [FirstName], [LastName], [Phone], [Email], [AccountType])
        SELECT   [Id],
                 [FirstName],
                 [LastName],
                 [Phone],
                 [Email],
                 [AccountType]
        FROM     [dbo].[Users]
        ORDER BY [Id] ASC;
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Users] OFF;
    END

DROP TABLE [dbo].[Users];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_Users]', N'Users';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Creating Procedure [dbo].[CreateBook]...';


GO
CREATE PROCEDURE [dbo].[CreateBook]
	@Id INT,
	@Title NVARCHAR(50),
	@Author NVARCHAR(50),
	@BookDescription NVARCHAR(50),
	@ReleaseYear INT,
	@LibraryId INT,
	@IsAvailable BIT = 1,
	@BorrowedByUserId INT = -1
AS
BEGIN
	INSERT INTO dbo.[Books] (Title, Author, BookDescription, ReleaseYear, LibraryId)
	VALUES (@Title, @Author, @BookDescription, @ReleaseYear, @LibraryId)
END
GO
PRINT N'Creating Procedure [dbo].[CreateUser]...';


GO
CREATE PROCEDURE [dbo].[CreateUser]
	@FirstName NVARCHAR(50),
	@LastName NVARCHAR(50),
	@Phone NVARCHAR(50),
	@Email NVARCHAR(50),
	@UserAddress NVARCHAR(50),
	@UserPassword NVARCHAR(50),
	@AccountType NVARCHAR(50)
AS
BEGIN
	INSERT INTO dbo.[Users] (FirstName, LastName, Phone, Email, UserAddress, UserPassword, AccountType)
	VALUES (@FirstName, @LastName, @Phone, @Email, @UserAddress, @UserPassword, @AccountType)
END
GO
PRINT N'Creating Procedure [dbo].[GetBooks]...';


GO
CREATE PROCEDURE [dbo].[GetBooks]
AS
BEGIN
	SELECT Id, Title, Author, BookDescription, ReleaseYear, IsAvailable
	FROM dbo.[Books]
END
GO
PRINT N'Creating Procedure [dbo].[GetLibraries]...';


GO
CREATE PROCEDURE [dbo].[GetLibraries]
AS
BEGIN
	SELECT Id, LibraryName
	FROM dbo.[Library]
END
GO
PRINT N'Update complete.';


GO
